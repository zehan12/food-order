export interface RestaurantType {
    email: string;
    password: string;
    confirmPassword?: string;
    restaurantName: string;
    city: string;
    address: string;
    contactNumber: string;
}